<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tenet.pia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".group.EditGroupActivity"></activity>
        <activity android:name=".contacts.AddContactsActivity" />
        <activity android:name=".notes.EditNotesActivity" />
        <activity android:name=".notes.AddNotesActivity" />
        <activity android:name=".notes.ShowNotesActivity" />
        <activity android:name=".schedule.EditScheduleActivity" />
        <activity android:name=".group.GroupContactsActivity" />
        <activity android:name=".group.GroupMainActivity" />
        <activity android:name=".schedule.AddScheduleActivity" />
        <activity android:name=".schedule.ShowScheduleActivity" />
        <activity android:name=".contacts.SelGroupActivity" />
        <activity android:name=".contacts.EditContactsActivity" />
        <activity android:name=".contacts.ContactsInfoActivity" />
        <activity android:name=".group.AddGroupActivity" />
        <activity android:name=".group.ShowGroupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>